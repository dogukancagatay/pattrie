[package]
name = "pattrie"
version = "0.1.0"
edition = "2021"

[lib]
name = "pattrie"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.27", features = ["extension-module"] }
prefix-trie = "0.4"
ipnet = "2"

[profile.release]
lto = true
codegen-units = 1
